# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'PerfectPath' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PerfectPath
    pod 'Firebase/Core'
    pod 'Firebase/Auth'
    pod 'Firebase/Database'
    pod 'Material'

  target 'PerfectPathTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PerfectPathUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'PerfectPath WatchKit App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PerfectPath WatchKit App

end

target 'PerfectPath WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PerfectPath WatchKit Extension

end
